#include <stdio.h>

/**
 * main: printsIt's name, then a new line
 * @argc: number of command line arguments
 * @argv: prints array of command line arguments
 * Return: Always 0 if successful
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf ("%s\n", argv [0]);
	return (0);
}
